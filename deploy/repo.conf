# Official source — all installs clone from this repository.
GITHUB_OWNER="ahmadfarzad-amiri"
GITHUB_REPO_NAME="wg"
GITHUB_BRANCH="main"
# jsDelivr CDN ref — pin to a release tag (avoid @latest; purge is unreliable).
# Bump this together with the git tag on each release.
GITHUB_CDN_REF="v1.0.22"
GITHUB_REPO_URL="https://github.com/ahmadfarzad-amiri/wg.git"
GITHUB_RAW_BASE="https://cdn.jsdelivr.net/gh/ahmadfarzad-amiri/wg@v1.0.22"
