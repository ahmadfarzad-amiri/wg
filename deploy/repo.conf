# Official source — all installs clone from this repository.
GITHUB_OWNER="ahmadfarzad-amiri"
GITHUB_REPO_NAME="wg"
GITHUB_BRANCH="main"
GITHUB_REPO_URL="https://github.com/${GITHUB_OWNER}/${GITHUB_REPO_NAME}.git"
# Version / CDN base are resolved at runtime:
#   WG_VERSION or GITHUB_CDN_REF / GITHUB_RAW_BASE / WG_RAW_BASE if set,
#   otherwise the latest GitHub release tag (see resolve_wg_version in lib/common.sh).
